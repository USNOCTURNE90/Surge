name: Convert and Push to Clash Repo

on:
  push:
    paths-ignore:
      - '.github/**'
  workflow_dispatch:

jobs:
  convert:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source repository
        uses: actions/checkout@v4
        with:
          path: source-repo

      - name: Checkout target repository
        run: |
          git clone https://oauth2:${{ secrets.PAT_TOKEN }}@github.com/usnocturne90/clash-auto.git clash-repo

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Convert Rules
        run: |
          python source-repo/.github/scripts/convert.py
        env:
          TARGET_DIR: clash-repo

      - name: Configure Git
        run: |
          cd clash-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
      - name: Commit and Push Changes
        run: |
          cd clash-repo
          git add .
          git commit -m "chore: update clash rules"
          git push
